%br
%h3 Add a new comment for #{@movie.title}
/ - comment = Comment.new
= form_for [@movie, @comment] do |f|
  .form-group
    = f.label :description
    = f.text_field :description, class: "form-control"
  .form-group
    = f.submit "Submit", class: "btn btn-default"